{
  "name": "pulse-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "Pulse HMI bridge — single persistent WECON-HMI websocket client that serves cold-storage temperatures over HTTP",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "puppeteer-core": "^25.3.0",
    "ws": "^8.18.0"
  }
}
